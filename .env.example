# toSub2 环境变量示例
# 用法：复制本文件为 .env，按需修改后保存。启动时自动加载，无需手动传环境变量。
# 注意：.env 已被 .gitignore 忽略，不会被提交；请勿把真实密码提交到仓库。
#       已存在的同名系统环境变量优先级更高，不会被本文件覆盖。

# ====================================================================
# 控制台访问密码（公网部署必填，本机使用可留空）
# ====================================================================
# 部署到公网或不可信网络时必须设置，否则任何打开页面的人都能拿到控制台令牌、
# 操作全部账号数据。本机或可信局域网使用时可留空（留空 = 无需密码直接访问）。
# 启用后：密码以 scrypt 哈希校验、不明文存储；同一 IP 连续输错 5 次锁定 15 分钟。
TOSUB2_CONSOLE_PASSWORD=

# ====================================================================
# 服务监听地址（一般不用改）
# ====================================================================
# 127.0.0.1（默认）：只允许本机访问。推荐配合 Nginx/Caddy 反向代理对外，更安全。
# 0.0.0.0：允许局域网/公网直接访问。仅当不使用反向代理、需要裸跑对外时才设置。
# 多数情况下保持默认即可（留空 = 用 127.0.0.1）。
# ONBOARDING_HOST=127.0.0.1

# ====================================================================
# 服务监听端口（默认 4399，一般不用改）
# ====================================================================
# ONBOARDING_PORT=4399

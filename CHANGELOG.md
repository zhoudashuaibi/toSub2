# 更新日志

## 未发布

### 新增：Outlook 邮箱取件

- 工具栏新增“Outlook 导入”按钮，用于导入使用 OAuth 刷新令牌收取验证码的 Outlook 邮箱。
- 每行格式：`邮箱----邮箱密码----clientId----refresh_token`，导入后登录到邮箱验证码步骤时自动调用取件接口收取并提交验证码。
- 自动收码采用三重过滤，确保只取最新一封 OpenAI 验证码：基准时间之后到达的邮件、发件人为 OpenAI/ChatGPT 相关域名、验证码 + 邮件标识去重；新码的几秒投递延迟由默认 2.5 秒轮询自然吸收。
- 取件接口默认 `https://8t92.cc/api/fetch-mails`，可在“Outlook 取件”栏位自定义为自建或其他接口。
- `refresh_token`、邮箱密码和 `clientId` 加密保存到系统安全凭据存储（Windows DPAPI / macOS Keychain），不写入任务元数据或协议日志。
- 与原有“邮件接收 API”收码路径互斥并存，现有逻辑不受影响。

### 新增：上传新号自动选择代理

- Sub2API 配置新增“新号自动选择绑定最少的代理”开关，默认开启。
- 未手动指定代理时，上传新号会分页读取全部 OpenAI 账号，按 `proxy_id` 统计每个可用（active）代理的绑定数，为每个账号独立选择当前绑定最少的代理；并列时随机。
- 批量上传多个号时在内存中累加本次分配，避免整批都选到同一个代理；停用的代理不参与选择。
- 手动指定代理后自动选择失效；自动统计失败时降级为不设置 `proxy_id`（由后端使用默认代理），不会阻断上传。

### 新增：上传时禁用账号自动暂停

- Sub2API 配置新增“禁用 5h 自动暂停”和“禁用 7d 自动暂停”两个独立开关，默认关闭。
- 勾选后上传的账号会在 `extra` 中设置 `auto_pause_5h_disabled` / `auto_pause_7d_disabled`，账号在对应滚动窗口用量超阈时不被 Sub2API 自动暂停；不勾选则不设置该字段，沿用后端默认行为。
- 与原有 `extra` 字段合并，不会覆盖账号导入文件里的其他扩展字段。

## v1.5.0 - 2026-08-10

这次版本新增 Sub2API 号池巡检和自动修复，完善代理风控处理、邮箱验证码重试、任务并发控制与服务关闭流程，并加入 PM2（Node.js 进程管理器）守护配置。

### 新增：Sub2API 号池监控

- 可以在 Sub2API 配置中启用每 5 分钟巡检，也可以随时点击“立即检查”。
- 按页读取 OpenAI 平台中状态为 `error` 的账号；配置号池后只检查所选号池，不选则检查全部账号。
- 按邮箱匹配本地任务，只对上次登录过程中密码、邮箱验证码和登录 2FA 均自动完成的账号执行自动修复。
- 手动输入绑定手机号或手机验证码不影响自动修复资格，因为账号通常只需绑定一次手机号。
- 巡检发现异常后自动重新登录、完成 Codex 授权，并按远端账号 ID 覆盖授权凭据，不会创建重复账号。
- 更新时保留远端账号已有的模型映射等配置，清除错误后重新开启调度。
- 更新 Sub2API 中途失败时记录待重传账号 ID；下一轮直接继续上传新授权，不重复登录，也不会因为远端账号已变成 `active` 而漏掉。
- 临时网络错误、代理失败、限流或后端短时不可用进入 5 分钟冷却，不会把账号永久排除。
- 明确返回账号封禁、删除或永久停用时，标记为永久跳过；用户仍可手动“重新登录并授权”解除标记并再次确认。
- 同一邮箱已有登录、授权、2FA 设置或巡检任务运行时不会重复启动。
- 前端显示监控开关、当前状态、上次检查时间、检查结果、冷却数量和永久跳过原因。
- 监控配置和运行状态保存在服务数据目录，服务重启后自动恢复。

### 代理与风控处理

- 代理首次检测通过后，正式登录或授权遇到明确安全校验页面时，先保持当前代理重试最多 3 次，仍失败才更换代理会话。
- `400/409` 只有同时出现安全校验响应头或真实 HTML 安全校验页时才按代理风控处理。
- 普通手机号不可用、`invalid_state` JSON 等业务错误不再错误触发代理轮换，只提示用户更换手机号。
- 登录、邮箱验证、2FA、手机号绑定、工作区选择、OAuth Token（授权令牌）交换和刷新请求统一使用风控重试策略。
- `curl_cffi` 不支持默认 `chrome146` 指纹时，会在创建 Session（请求会话）和实际发送请求两个阶段自动降级到兼容的 Chrome（谷歌浏览器）指纹。

### 登录和验证码修复

- 手动输入错误邮箱验证码后不再结束任务，会提示重新输入，也可以重新发送验证码。
- 修复错误邮箱验证码后再次输入时，旧的输入解析缓存可能干扰新验证码的问题。
- 邮箱收码 API（接口）的单次请求超时由 12 秒延长到 60 秒，兼容响应较慢的邮件服务。
- 手机号验证码发送接口保留“先带 `channel`、必要时去掉 `channel` 再提交”的兼容流程。
- Sub2API 返回错误时优先提取服务端 `message` 字段，便于区分后端错误和本地请求错误。

### 任务可靠性

- 重新登录、重新授权、重试、取消、验证码输入、接码取号、设置 2FA 和自动巡检统一按邮箱加锁，修复并发点击可能启动多个子进程的问题。
- 批量操作同样使用邮箱锁，正在处理的账号会返回明确冲突，不会重复增加尝试次数。
- Sub2API 批量上传的账号明确设置为 `active` 且启用调度，修复上传后账号不可调度的问题。
- 服务关闭时停止巡检定时器，取消进行中的 Sub2API 请求，等待自动修复、登录任务和元数据保存完成后再退出。
- 修复 Sub2API 请求永久挂起时，服务收到 `SIGTERM`（终止信号）后无法及时退出的问题。

### 进程守护

- 新增 PM2 配置和 `daemon:start`、`daemon:restart`、`daemon:stop`、`daemon:logs` 命令。
- 支持异常退出自动重启、逐步延迟重启、最大重启次数和优雅关闭等待时间。
- 支持通过 `ONBOARDING_HOST`、`ONBOARDING_PORT` 和 `ONBOARDING_OUTPUT_ROOT` 保留原数据目录并开放局域网访问。
- README 增加 macOS、Linux 和 Windows PowerShell（命令行）的 PM2 使用说明。

### 测试

- 新增号池巡检、自动修复资格、永久停用跳过、冷却、远端更新中断重传和启用调度测试。
- 新增同邮箱并发重新登录测试，确认只启动一个子进程。
- 新增错误邮箱验证码重新输入、普通手机号错误和 HTML 风控页区分测试。
- 新增同一代理重试 3 次、普通 `400/409` 不重试以及 TLS 指纹创建阶段降级测试。
- 新增服务关闭时取消挂起巡检请求的测试。

### 升级说明

- 可以从 `v1.4.0` 直接升级，原任务目录和授权文件不需要迁移。
- 号池监控默认关闭，需要在 Sub2API 配置中主动启用。
- PM2 是可选组件；需要进程异常退出后自动恢复巡检时再安装和启用。

## v1.3.1 - 2026-08-09

### 修复

- 修复 Linux 没有系统安全凭据存储时，密码、2FA 密钥或代理只能在当前进程使用，却被错误拒绝为 HTTP 501 的问题。
- 服务重启后，如果排队任务缺少原密码、2FA 密钥或代理，会停止自动启动并明确提示用户重新导入，避免使用不完整资料登录。

## v1.3.0 - 2026-08-09

这次版本增加 Python TLS 指纹请求层和账号代理支持，并修复登录状态、代理恢复、重复任务及运行中资料更新的问题。

### 新增

- 接入 Python `curl_cffi 0.15.0` TLS 指纹请求层，ChatGPT、auth.openai.com、Sentinel 和 OAuth Token 请求统一复用浏览器指纹会话。
- 新增账号 HTTP/HTTPS、SOCKS5/SOCKS5h 代理配置；每个任务支持随机轮换 `sid`，代理检测失败最多重试 10 次。
- 新增浏览器本地保存代理配置，创建任务、重试和重新授权时使用当前最新配置。
- macOS 使用 Keychain（钥匙串）、Windows 使用 DPAPI（数据保护接口）加密保存任务代理，服务重启后可以继续使用原代理。
- 增加 Python 依赖安装说明、TLS 请求层测试和 Windows/Linux 自动检查依赖。

### 修复

- 修复 Sentinel（安全令牌）请求通过 TLS 指纹层时缺少 JSON 解析方法，导致账号资料补全被误报为安全校验失败的问题。
- 修复 Python Cookie 有效期使用秒、Node.js 使用毫秒，导致有效登录 Cookie 被提前删除的问题。
- 修复服务重启后排队任务丢失代理并悄悄改用本机网络的问题；无法安全恢复代理时会等待用户重新确认。
- 修复多个请求同时添加相同邮箱时可能创建并运行多个重复任务的问题。
- 修复运行中更新密码、邮箱收码接口、2FA 密钥或代理后，旧登录进程仍继续使用旧资料的问题。
- 修复更新任务时旧的准备流程和新的准备流程可能同时启动两个登录子进程的问题。
- 修复手动补填密码时覆盖并删除已保存代理的问题。

## v1.2.0 - 2026-08-08

这次版本补齐了从账号授权完成到直接写入 Sub2API 号池的流程，同时扩展了短信接码平台和账号导入格式，并修复配置弹窗内容溢出的问题。

### 新增

- 新增自定义接码号码池，支持批量粘贴“手机号 + 接码 API”，并按顺序为并发任务分配号码。
- 自定义接码会在发送短信前记录旧验证码，只提交接口中后续出现的新 6 位验证码。
- 批量导入支持“邮箱 + 密码 + 邮件接收 API”和“邮箱 + 密码 + 邮件接收 API + 2FA 密钥”，并可按原格式导出。
- 集成 SMSBower，支持按服务代码和国家 ID 自动取号、收取验证码、完成或取消订单。
- SMSBower 支持查询 OpenAI 实时价格和库存，按价格排序选择国家，并在取号时限制最高价格。
- 接码平台配置改为独立配置弹窗，不同平台的配置分别保存在当前浏览器。
- 服务端增加统一 SmsProvider（短信平台适配器），后续接入其他平台时可以复用取号、轮询、换号和错误处理流程。
- 保留旧 LubanSMS 任务元数据和旧取号接口的兼容读取。
- 新增 Sub2API 直传功能，支持单个上传、跨页批量上传、目标号池选择和默认号池导入。
- 新增 Sub2API 后端号池读取，使用管理员 API Key（`x-api-key`）认证；配置只保存在当前浏览器。
- Sub2API 上传配置支持多选号池、选择代理，以及统一设置并发数、负载因子、优先级和允许使用的模型；空值会保留账号原配置。
- Sub2API 配置弹窗支持一次读取号池和代理列表，并兼容旧版单个 `groupId` 配置。

## v1.1.1 - 2026-08-07

这次修复把登录阶段的工作区选择补齐了，避免有些账号在密码、邮箱验证码或 2FA 之后，卡在工作区页或被误报成会话失效。

### 修复

- 修复 ChatGPT 登录后出现工作区页时，脚本没有先完成工作区选择的问题。
- 修复没有可选工作区的账号被强制报错的问题，改为直接跳过。
- 兼容 Codex 选中账号后直接返回授权结果、不需要选择工作区的账号。
- 修复 Codex 授权页真正回到登录页时，错误信息被误判成 `SESSION_SELECTION_INVALID` 的问题。
- 增加密码登录、邮箱验证码登录、2FA 和工作区跳过的回归测试。

## v1.1.0 - 2026-08-07

这是 toSub2 的首个正式 GitHub Release（版本发布），重点补齐 Windows 凭据持久化和跨平台验证。

### 新增

- Windows 使用当前用户的 DPAPI（数据保护接口）加密保存密码和 2FA 密钥。
- Windows 凭据密文保存在 `%LOCALAPPDATA%\toSub2\credentials`，只能由同一个 Windows 用户解密。
- 增加 Windows 和 Ubuntu 的 GitHub Actions（自动检查）。
- 增加网页控制台完整流程测试，覆盖任务创建、子进程启动、重新授权、下载和删除。
- 增加协议完整流程测试，覆盖邮箱验证码、手机号、短信验证码、检查点覆盖和 OAuth 文件刷新。

### 修复

- 修复 Windows 添加密码或 2FA 密钥时错误提示只能使用 macOS Keychain（钥匙串）的问题。
- 修复 Windows PowerShell（命令行）未加载 `System.Security`（系统安全程序集）导致 DPAPI 无法调用的问题。
- 修复 Windows 下中文或特殊字符密码可能受 PowerShell 默认编码影响的问题。
- 修复批量删除任务时，后台元数据写入与目录删除冲突，偶发返回 `ENOTEMPTY` 或 HTTP 500 的问题。
- 修复已删除任务仍可能继续写入 `job-meta.json` 的问题。

### 跨平台验证

- Windows 已验证 DPAPI 凭据保存、重启读取、覆盖更新和删除。
- Windows 已验证网页服务启动、Node.js 子进程和 UTF-8 页面输出。
- Windows 已验证登录检查点多次覆盖和 OAuth 导入文件原文件刷新。
- Windows 和 Ubuntu 均已通过完整自动检查。

### 已知限制

- Linux 可以使用邮箱验证码登录、Codex OAuth（授权登录）、手机号绑定和文件导出。
- Linux 暂不持久保存密码和 2FA 密钥，因为不同 Linux 环境不一定具备统一可用的系统凭据服务。
- Windows 使用 `--host 0.0.0.0` 开放局域网访问时，可能需要在系统防火墙中允许 Node.js 访问专用网络。

### 升级方式

```bash
git pull
npm install
npm run dev
```
